# 快速入门指南

## 🚀 5分钟快速添加新场景

### 步骤1：准备图片

将你的图片放入 `images/pub/infinidepth/` 目录：
```
images/pub/infinidepth/
├── scene1_rgb.png          # RGB图片
├── scene1_method1.png      # 方法1的深度图
├── scene1_method2.png      # 方法2的深度图
├── scene1_method3.png      # 方法3的深度图
└── scene1_method4.png      # 方法4的深度图
```

### 步骤2：编辑配置文件

打开 `js/infinidepth/config.js`，找到 `scenes` 数组，添加你的场景：

```javascript
scenes: [
    // ...已有场景...
    
    // 添加你的新场景（注意前面要加逗号）
    ,{
        name: '你的场景名称',
        rgbImage: 'images/pub/infinidepth/scene1_rgb.png',
        depthImages: [
            'images/pub/infinidepth/scene1_method1.png',
            'images/pub/infinidepth/scene1_method2.png',
            'images/pub/infinidepth/scene1_method3.png',
            'images/pub/infinidepth/scene1_method4.png'
        ],
        methodLabels: [
            'Depth Anything v2',
            'Metric3D v2',
            'DepthPro',
            'InfiniDepth (Ours)'
        ]
    }
]
```

### 步骤3：刷新页面

保存文件后，刷新浏览器即可看到新场景！

---

## 📋 配置模板

复制以下模板快速添加场景：

```javascript
{
    name: 'Scene Name',  // 必填：场景名称
    rgbImage: 'images/pub/infinidepth/your_rgb.png',  // 必填：RGB图片路径
    depthImages: [  // 必填：4张深度图路径
        'images/pub/infinidepth/depth1.png',
        'images/pub/infinidepth/depth2.png',
        'images/pub/infinidepth/depth3.png',
        'images/pub/infinidepth/depth4.png'
    ],
    methodLabels: [  // 可选：方法名称（默认为Method 1-4）
        'Method 1',
        'Method 2',
        'Method 3',
        'Method 4 (Ours)'
    ]
}
```

---

## 🎨 自定义动画速度

在 `config.js` 中修改：

```javascript
transitionDuration: 500  // 单位：毫秒（默认500ms）
```

建议值：
- 快速切换：300-400ms
- 标准速度：500ms（推荐）
- 平滑切换：600-800ms

---

## 🔍 自定义缩放参数

```javascript
initialPatchSize: 256,  // 初始查看区域大小
minPatchSize: 64,       // 最小（最大放大）
maxPatchSize: 512,      // 最大（最小放大）
zoomStep: 0.1,          // 滚轮缩放步长（0.05-0.2）
```

---

## ✅ 检查清单

添加新场景前，请确认：

- [ ] RGB图片和深度图都已上传到正确目录
- [ ] 图片路径拼写正确（区分大小写）
- [ ] 配置对象格式正确（注意逗号和引号）
- [ ] 每个场景有4张深度图
- [ ] methodLabels数组有4个元素

---

## 🐛 常见问题

### Q: 切换按钮不工作？
A: 检查浏览器控制台是否有JavaScript错误，确认config.js中的场景配置格式正确。

### Q: 图片不显示？
A: 
1. 检查图片路径是否正确
2. 打开浏览器开发者工具 → Network，查看图片是否加载失败
3. 确认图片文件名大小写匹配

### Q: 动画卡顿？
A: 
1. 压缩图片大小（建议单张 < 3MB）
2. 减小 transitionDuration 值
3. 使用 WebP 格式图片

### Q: 只有一个场景，按钮显示灰色？
A: 这是正常的，单场景时切换按钮会自动禁用。

---

## 💡 提示

1. **批量添加场景**：可以写一个简单的脚本来生成配置
2. **图片命名规范**：建议使用统一的命名格式，如 `scene_name_method.png`
3. **测试新场景**：添加后先测试切换和放大镜功能是否正常
4. **备份配置**：修改前建议备份 config.js 文件

---

## 📞 需要帮助？

详细文档请查看：`js/infinidepth/README.md`
